import { StyleSheet } from "react-native";

export const surveyModelStyle = StyleSheet.create(
{
    container: {
        width: '100%',
        marginBottom: '5%',
        borderBottomColor: 'black',
        paddingBottom: '5%',
        borderBottomWidth: 2,
    },
    buttonContainer: {
        marginBottom: '2%',
    }
});