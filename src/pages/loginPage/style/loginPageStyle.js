import { StyleSheet } from "react-native";

export const loginPageStyle = StyleSheet.create(
    {
        container: {
            marginTop: 'auto',
            marginBottom: 'auto',
        }
    }
);